Ant 1.5
In the SIR repository this is version v4.
- Several classes were changed to run in java 7. 
- Some other classes were excluded (files finished as java.saeg)
- Several tests were commented and ignored
- This version already contains the fault DEW_AK_1 in the file DOMElementWriter, see DOMElementWriter.cpp.

Installing
- copy the folder into your workspace
- create a project with the folder's name
- keep only src/main ans src/testcases as source folders. Exclude all other folders from the build path
- configure output folder properties for the source folders: {project_name}/build/classes for src/main and {project_name}/build/test-classes for src/testcases
- include all external jars of lib and lib/optional folders in the build path
- include JUnit library as JUnit4
